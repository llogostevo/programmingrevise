import { buildLesson } from "@/data/lesson-builder";
import type { CourseUnit } from "@/types/course";

export const proceduresFunctionsUnit: CourseUnit = {
  slug: "procedures-functions",
  number: 9,
  title: "Procedures & functions",
  specReference: "2.2.3",
  description: "Break programs into reusable subroutines with parameters, returned values and clear scope.",
  accent: "teal",
  status: "available",
  plannedLessons: [],
  lessons: [
    buildLesson({
      slug: "procedures-defining-calling",
      title: "Defining and calling procedures",
      shortDescription: "Put a named task into a reusable subroutine and call it when it is needed.",
      objective: "Define and call a procedure in OCR ERL and Python.",
      estimatedMinutes: 25,
      specReference: "2.2.3",
      vocabulary: [
        { term: "Sub-program", definition: "A named, reusable block of code that performs a specific task." },
        { term: "Subroutine", definition: "Another name for a sub-program, such as a procedure or function." },
        { term: "Procedure", definition: "A sub-program that performs a task without returning a value." },
        { term: "Call", definition: "An instruction that runs a named sub-program." },
      ],
      explanation: "A procedure groups instructions for one clear task. ERL starts it with procedure and ends it with endprocedure. Python uses def for both procedures and functions. Defining a procedure stores its instructions, but those instructions do not run until the procedure is called. Calls let the same code be reused without copying it.",
      keyPoints: [
        "A procedure performs a task but does not return a value.",
        "ERL uses procedure ... endprocedure; Python uses def, a colon and indentation.",
        "The procedure name should describe its task.",
        "A definition must be followed by a call before its body executes.",
        "One procedure can be called many times.",
      ],
      examTip: "If OCR asks you to use a procedure, include both its complete definition and an appropriate call in the main program.",
      commonMistake: "Defining a procedure is not the same as running it. A procedure with no call produces no output.",
      examples: [
        {
          title: "Greet a player",
          context: "Put a greeting into a procedure and call it twice.",
          erl: 'procedure greetPlayer()\n    print("Welcome, player!")\nendprocedure\n\ngreetPlayer()\ngreetPlayer()',
          python: 'def greet_player():\n    print("Welcome, player!")\n\ngreet_player()\ngreet_player()',
          notes: [
            { line: "1–3", explanation: "The procedure is defined, but its print instruction has not run yet." },
            { line: "5", explanation: "The first call runs the body and prints one greeting." },
            { line: "6", explanation: "The second call reuses the same body and prints it again." },
          ],
        },
        {
          title: "Show the rules",
          context: "Keep a two-line set of game rules together.",
          erl: 'procedure showRules()\n    print("Collect five stars")\n    print("Avoid the walls")\nendprocedure\n\nprint("Game starting")\nshowRules()',
          python: 'def show_rules():\n    print("Collect five stars")\n    print("Avoid the walls")\n\nprint("Game starting")\nshow_rules()',
          notes: [
            { line: "1–3", explanation: "Both rule messages belong to the same named task." },
            { line: "5", explanation: "The main program starts by printing its own message." },
            { line: "6", explanation: "The call transfers control to the procedure body." },
          ],
        },
      ],
      predict: {
        language: "python",
        code: 'def cheer():\n    print("Go team!")\n\nprint("Ready")\ncheer()\ncheer()',
        expectedOutput: "Ready\nGo team!\nGo team!",
      },
      debug: {
        language: "python",
        code: 'def show_score():\n    print("Score: 10")',
        errorKind: "logic",
        expectedFix: 'def show_score():\n    print("Score: 10")\n\nshow_score()',
        feedback: "The definition is valid, but the body never runs until show_score() is called.",
      },
      complete: {
        language: "erl",
        instructions: "Complete the keyword that closes the ERL procedure.",
        codeParts: ['procedure showMessage()\n    print("Keep going")\n', "\n\nshowMessage()"],
        answers: [["endprocedure"]],
      },
      guided: {
        title: "Scaffolded practice",
        instructions: "Define a Python procedure named show_result with no parameters. It must print Result ready. Call the procedure once.",
        starterCode: '# Define the procedure\ndef show_result():\n    \n\n# Call it\n',
        exampleOutput: "Result ready",
        hiddenTests: [{ expectedOutput: "Result ready" }],
        requiredConstructs: ["function", "procedure-output", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. You defined the procedure and called it so its output was produced.",
      },
      independent: {
        title: "Independent task",
        instructions: "Define a procedure named match_intro that prints Match starting and then Teams ready on the next line. Call that same procedure twice so its two-line output is reused.",
        starterCode: "",
        exampleOutput: "Match starting\nTeams ready\nMatch starting\nTeams ready",
        hiddenTests: [{ expectedOutput: "Match starting\nTeams ready\nMatch starting\nTeams ready" }],
        requiredConstructs: ["function", "procedure-output", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. Two calls reused both instructions in the same procedure.",
      },
      review: {
        question: "Identify what happens when a procedure is defined but never called.",
        options: [
          "Its body runs once automatically",
          "Its body does not execute",
          "It returns its final printed value",
          "It becomes a global variable",
        ],
        answer: 1,
        correctFeedback: "Correct. A definition describes the subroutine; a call runs it.",
      },
      retrievalPrompt: "State one reason a loop is useful when processing an array.",
      examCallout: "OCR may ask you to complete procedure/endprocedure syntax, trace calls or decompose repeated instructions into a named procedure.",
    }),
    buildLesson({
      slug: "procedures-using-parameters",
      title: "Using parameters",
      shortDescription: "Pass changing data into one reusable subroutine.",
      objective: "Define and call a procedure that uses every value supplied through its parameters.",
      estimatedMinutes: 27,
      specReference: "2.2.3",
      vocabulary: [
        { term: "Parameter", definition: "A named value received by a sub-program when it is called." },
        { term: "Parameter list", definition: "The parameter names written inside the brackets of a sub-program definition." },
        { term: "Call", definition: "An instruction that runs a sub-program and supplies any required values." },
        { term: "Reuse", definition: "Calling the same sub-program with different values instead of copying its code." },
      ],
      explanation: "Parameters make one procedure useful with many values. They are named in the definition and receive the values supplied by each call. The body should use those parameters directly. If it asks for replacement input, it throws away the caller's data and defeats the purpose of passing parameters.",
      keyPoints: [
        "Values in a call match parameters by position.",
        "A string supplied directly in a call needs quotation marks.",
        "The body must use its parameters rather than overwrite them.",
        "Parameters are local to the sub-program.",
        "Clear parameter names make the sub-program easier to understand.",
      ],
      examTip: "When a question gives a procedure heading such as SaveLogs(data, filename), use data and filename inside the body; do not ask the user for them again.",
      commonMistake: "Do not write name = input() inside greet(name). That overwrites the parameter that the call already supplied.",
      examples: [
        {
          title: "Personalise a greeting",
          context: "Call one procedure with two different names.",
          erl: 'procedure greet(name)\n    print("Hello, " + name + "!")\nendprocedure\n\ngreet("Asha")\ngreet("Leo")',
          python: 'def greet(name):\n    print("Hello, " + name + "!")\n\ngreet("Asha")\ngreet("Leo")',
          notes: [
            { line: "1", explanation: "name is the parameter that receives each call's string." },
            { line: "2", explanation: "The body uses the parameter in its output." },
            { line: "5–6", explanation: "Each quoted value produces a different greeting." },
          ],
        },
        {
          title: "Display a score",
          context: "Supply a player and a numeric score to one procedure.",
          erl: 'procedure displayScore(player, score)\n    print(player + ": " + str(score))\nendprocedure\n\ndisplayScore("Mia", 7)',
          python: 'def display_score(player, score):\n    print(player + ": " + str(score))\n\ndisplay_score("Mia", 7)',
          notes: [
            { line: "1", explanation: "The two call values are matched to player and score in order." },
            { line: "2", explanation: "score is cast to a string before concatenation." },
            { line: "4", explanation: "The call supplies a string and an integer." },
          ],
        },
      ],
      predict: {
        language: "erl",
        code: 'procedure announce(team, goals)\n    print(team + " scored " + str(goals))\nendprocedure\n\nannounce("Rovers", 3)',
        expectedOutput: "Rovers scored 3",
      },
      debug: {
        language: "python",
        code: 'def greet(name):\n    name = input("Name: ")\n    print("Hello, " + name)\n\ngreet("Asha")',
        errorKind: "logic",
        expectedFix: 'def greet(name):\n    print("Hello, " + name)\n\ngreet("Asha")',
        feedback: "The procedure must use the name parameter passed by the call, not replace it with a new input.",
      },
      complete: {
        language: "erl",
        instructions: "Complete the call by supplying the string value correctly.",
        codeParts: ['procedure cheer(team)\n    print("Go " + team)\nendprocedure\n\ncheer(', ")"],
        answers: [['"Falcons"']],
      },
      guided: {
        title: "Scaffolded practice",
        instructions: "Define show_points(player, points), which prints the player, a space, and the points. In the main program, read two different player/points pairs and call the same procedure once for each pair, in input order.",
        starterCode: 'def show_points(player, points):\n    # Use both parameters in this output\n    \n\nplayer_one = input("First player: ")\npoints_one = int(input("First points: "))\nplayer_two = input("Second player: ")\npoints_two = int(input("Second points: "))\n\n# Call the same procedure with each pair\n',
        exampleInput: ["Mia", "7", "Leo", "2"],
        exampleOutput: "Mia 7\nLeo 2",
        hiddenTests: [
          { input: ["Kai", "0", "Asha", "12"], expectedOutput: "Kai 0\nAsha 12" },
          { input: ["Mia Chen", "-4", "Rovers", "99"], expectedOutput: "Mia Chen -4\nRovers 99" },
        ],
        requiredConstructs: ["function", "parameter", "procedure-output", "input", "int", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. Both calls passed their own changing parameter values into one reusable procedure.",
      },
      independent: {
        title: "Independent task",
        instructions: "Define announce_result(team, goals). It must print team + ' scored ' + goals. Read the team and integer goals in the main program, then call the procedure.",
        starterCode: "",
        exampleInput: ["Rovers", "3"],
        exampleOutput: "Rovers scored 3",
        hiddenTests: [
          { input: ["City", "0"], expectedOutput: "City scored 0" },
          { input: ["Falcons", "11"], expectedOutput: "Falcons scored 11" },
          { input: ["North Stars", "21"], expectedOutput: "North Stars scored 21" },
        ],
        requiredConstructs: ["function", "parameter", "procedure-output", "input", "int", "concatenation", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. You used the supplied parameters and passed all three calls.",
      },
      review: {
        question: "Complete the call that passes the text Red into freeseats.",
        options: ["freeseats(Red)", 'freeseats("Red")', 'freeseats = "Red"', "freeseats() = Red"],
        answer: 1,
        correctFeedback: "Correct. A literal string supplied in a call needs quotation marks.",
      },
      retrievalPrompt: "Give the operator used to concatenate strings in OCR ERL and Python.",
      examCallout: "OCR frequently supplies a sub-program heading and awards marks for using its parameters exactly as given. Overwriting them with input loses the intended data flow.",
    }),
    buildLesson({
      slug: "functions-returning-values",
      title: "Functions and returned values",
      shortDescription: "Calculate a value inside a function, return it and store it in the caller.",
      objective: "Write a function that returns a result and use the returned value in the main program.",
      estimatedMinutes: 28,
      specReference: "2.2.3",
      vocabulary: [
        { term: "Function", definition: "A sub-program that returns a value." },
        { term: "Return", definition: "Send a value from a function back to the instruction that called it." },
        { term: "Returned value", definition: "The result supplied by a completed function call." },
        { term: "Assignment", definition: "Storing a value, including a returned value, in a variable." },
      ],
      explanation: "A function produces a value for the rest of the program. ERL encloses it with function and endfunction; Python uses def. In both languages, return sends the result back to the call. Printing inside a function only displays something—it does not give the caller a value to store or reuse.",
      keyPoints: [
        "A function must use return to send its result to the caller.",
        "Store a returned value with assignment, such as total = calculateTotal(4, 3).",
        "print and return have different jobs.",
        "Code after an executed return in the same function is not reached.",
        "A returned value can be printed, compared or used in another calculation.",
      ],
      examTip: "If a question asks for a function, return the result. Then show a separate call such as result = newPrice(5, \"premium\").",
      commonMistake: "print(total) inside a function displays the result but returns no usable value. Write return total and print it later if required.",
      examples: [
        {
          title: "Return a square",
          context: "Calculate a square and store the result of the call.",
          erl: 'function square(number)\n    answer = number ^ 2\n    return answer\nendfunction\n\nresult = square(6)\nprint(result)',
          python: 'def square(number):\n    answer = number ** 2\n    return answer\n\nresult = square(6)\nprint(result)',
          notes: [
            { line: "1", explanation: "number receives 6 from the call." },
            { line: "2", explanation: "The local calculation produces 36." },
            { line: "3", explanation: "return sends 36 back to the call, where result stores it." },
          ],
        },
        {
          title: "Calculate a room price",
          context: "Return a different total for basic and premium rooms.",
          erl: 'function newPrice(nights, room)\n    if room == "basic" then\n        price = 60 * nights\n    else\n        price = 80 * nights\n    endif\n    return price\nendfunction\n\nresult = newPrice(5, "premium")\nprint(result)',
          python: 'def new_price(nights, room):\n    if room == "basic":\n        price = 60 * nights\n    else:\n        price = 80 * nights\n    return price\n\nresult = new_price(5, "premium")\nprint(result)',
          notes: [
            { line: "1", explanation: "Two parameters provide the number of nights and room type." },
            { line: "2–5", explanation: "Selection chooses one calculation." },
            { line: "7", explanation: "The chosen price is returned rather than printed inside the function." },
          ],
        },
      ],
      predict: {
        language: "python",
        code: "def add_bonus(score):\n    score = score + 5\n    return score\n\nnew_score = add_bonus(8)\nprint(new_score)",
        expectedOutput: "13",
      },
      debug: {
        language: "python",
        code: "def double(number):\n    answer = number * 2\n    print(answer)\n\nresult = double(4)\nprint(result)",
        errorKind: "logic",
        expectedFix: "def double(number):\n    answer = number * 2\n    return answer\n\nresult = double(4)\nprint(result)",
        feedback: "Printing answer does not return it. return answer lets result store 8 instead of None.",
      },
      complete: {
        language: "erl",
        instructions: "Complete the keyword that sends the calculated value back to the caller.",
        codeParts: ["function triple(number)\n    answer = number * 3\n    ", " answer\nendfunction"],
        answers: [["return"]],
      },
      guided: {
        title: "Scaffolded practice",
        instructions: "Define double_number(number) and return number multiplied by 2. Read an integer, call the function, store that returned value in a main-program variable named result, then print result.",
        starterCode: 'def double_number(number):\n    answer = number * 2\n    # Return the answer\n    \n\nnumber = int(input("Number: "))\nresult = \nprint(result)',
        exampleInput: ["6"],
        exampleOutput: "12",
        hiddenTests: [
          { input: ["0"], expectedOutput: "0", expectedVariables: { result: 0 } },
          { input: ["-4"], expectedOutput: "-8", expectedVariables: { result: -8 } },
          { input: ["21"], expectedOutput: "42", expectedVariables: { result: 42 } },
          { input: ["101"], expectedOutput: "202", expectedVariables: { result: 202 } },
        ],
        requiredConstructs: ["function", "parameter", "return", "return-used", "input", "int", "arithmetic", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. Your function returned a reusable value for every test number.",
      },
      independent: {
        title: "Independent task",
        instructions: "Define calculate_change(paid, cost), which returns paid minus cost in that order. Read paid and cost as integers, call the function, store its returned value in result and print result.",
        starterCode: "",
        exampleInput: ["20", "13"],
        exampleOutput: "7",
        hiddenTests: [
          { input: ["10", "10"], expectedOutput: "0", expectedVariables: { result: 0 } },
          { input: ["50", "18"], expectedOutput: "32", expectedVariables: { result: 32 } },
          { input: ["8", "11"], expectedOutput: "-3", expectedVariables: { result: -3 } },
        ],
        requiredConstructs: ["function", "parameter", "return", "return-used", "input", "int", "arithmetic", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. The parameter order flowed through the called function and result stored every returned change value.",
      },
      review: {
        question: "Identify the line a value-returning function needs.",
        options: ["print(total)", "return total", "input(total)", "global total"],
        answer: 1,
        correctFeedback: "Correct. return sends total back to the calling instruction.",
        incorrectFeedback: { "0": "Printing displays a value but does not return it to the caller." },
      },
      extraExercises: [
        {
          id: "functions-return-translate",
          exercise_type: "translate",
          stage: "complete",
          title: "Translate a value-returning function",
          instructions: "Translate the OCR ERL into Python. Preserve the parameter, returned value, input and call.",
          hints: [
            "Python uses def, a colon and indentation for the function body.",
            "Keep return inside the function, then call the function from the main program.",
            "Use def double(number):, return number * 2, then store double(number) before printing.",
          ],
          knowledgeWeight: 0.45,
          applicationWeight: 0.55,
          language: "erl",
          code: "function double(number)\n    return number * 2\nendfunction\n\nnumber = int(input())\nresult = double(number)\nprint(result)",
          starterCode: "def double(number):\n    # Return the doubled value\n    \n\nnumber = int(input())\nresult = \nprint(result)",
          hiddenTests: [
            { input: ["6"], expectedOutput: "12" },
            { input: ["0"], expectedOutput: "0" },
            { input: ["-4"], expectedOutput: "-8" },
          ],
          requiredConstructs: ["function", "parameter", "return", "return-used", "input", "int", "arithmetic", "print"],
        },
      ],
      retrievalPrompt: "State the purpose of a parameter in a sub-program.",
      examCallout: "OCR tests whether you can define functions, pass parameters, return results and replace inline calculations with calls such as totalPay = calculatePay(experience, miles).",
    }),
    buildLesson({
      slug: "functions-local-global-scope",
      title: "Local and global variables",
      shortDescription: "Understand where a variable exists and choose parameters over unnecessary global state.",
      objective: "Distinguish local and global scope and modify a global variable using the correct syntax.",
      estimatedMinutes: 28,
      specReference: "2.2.3",
      vocabulary: [
        { term: "Scope", definition: "The part of a program in which a variable can be accessed." },
        { term: "Local variable", definition: "A variable created inside a sub-program and accessible only there." },
        { term: "Global variable", definition: "A variable declared outside sub-programs and accessible throughout the program." },
        { term: "Lifetime", definition: "The time for which a variable exists in memory." },
      ],
      explanation: "Variables created inside a sub-program are local: they can be used only within that sub-program and are removed when it finishes. Parameters are local too. A global variable is declared in the main program. ERL marks it with global; Python uses global inside a function when that function changes the global value. Prefer parameters and returned values because widespread global state is harder to debug.",
      keyPoints: [
        "Local variables cannot be accessed outside the sub-program that created them.",
        "Parameters are local variables.",
        "ERL declares a global with global name = value.",
        "Python needs global name inside a function before assigning to that global.",
        "Local data and parameters reduce naming conflicts and hidden changes.",
      ],
      examTip: "When explaining scope, say where the variable is declared, where it can be accessed and when it is removed from memory.",
      commonMistake: "A local calculation cannot be printed later by name from the main program. Return it and store the result instead.",
      examples: [
        {
          title: "Keep tax local",
          context: "Return a local calculation so the caller can use it.",
          erl: 'function calculateTax(price)\n    tax = price * 0.2\n    return tax\nendfunction\n\nresult = calculateTax(100)\nprint(result)',
          python: 'def calculate_tax(price):\n    tax = price * 0.2\n    return tax\n\nresult = calculate_tax(100)\nprint(result)',
          notes: [
            { line: "1", explanation: "price is a local parameter." },
            { line: "2", explanation: "tax is also local and exists only during this call." },
            { line: "3", explanation: "Returning tax makes its value available without exposing the local name." },
          ],
        },
        {
          title: "Update a global score",
          context: "Allow a procedure to change one score shared by the main program.",
          erl: 'global score = 0\n\nprocedure addPoints(points)\n    score = score + points\nendprocedure\n\naddPoints(10)\naddPoints(5)\nprint(score)',
          python: 'score = 0\n\ndef add_points(points):\n    global score\n    score = score + points\n\nadd_points(10)\nadd_points(5)\nprint(score)',
          notes: [
            { line: "1", explanation: "score is created outside the sub-program and is global." },
            { line: "3–5", explanation: "The procedure changes the shared score using its local points parameter." },
            { line: "7–8", explanation: "Both calls update the same stored score." },
          ],
        },
      ],
      predict: {
        language: "python",
        code: "score = 2\n\ndef add_points(points):\n    global score\n    score = score + points\n\nadd_points(3)\nprint(score)",
        expectedOutput: "5",
      },
      debug: {
        language: "python",
        code: "def calculate_tax(price):\n    tax = price * 0.2\n\ncalculate_tax(100)\nprint(tax)",
        errorKind: "runtime",
        expectedFix: "def calculate_tax(price):\n    tax = price * 0.2\n    return tax\n\nresult = calculate_tax(100)\nprint(result)",
        feedback: "tax is local and cannot be accessed in the main program. Return its value and store the function call instead.",
      },
      complete: {
        language: "erl",
        instructions: "Complete the keyword that declares score as a global variable.",
        codeParts: ["", " score = 0\n\nprocedure resetScore()\n    score = 0\nendprocedure"],
        answers: [["global"]],
      },
      guided: {
        title: "Scaffolded practice",
        instructions: "Define add_bonus(score), create a local variable named result that adds 5, and return result. Read an integer in the main program, call the function, store its returned value in a main-program variable named new_score and print new_score.",
        starterCode: 'def add_bonus(score):\n    # result is local\n    result = \n    return \n\nscore = int(input("Score: "))\nnew_score = \nprint(new_score)',
        exampleInput: ["8"],
        exampleOutput: "13",
        hiddenTests: [
          { input: ["0"], expectedOutput: "5", expectedVariables: { new_score: 5 } },
          { input: ["20"], expectedOutput: "25", expectedVariables: { new_score: 25 } },
          { input: ["-4"], expectedOutput: "1", expectedVariables: { new_score: 1 } },
        ],
        requiredConstructs: ["function", "parameter", "return", "return-used", "input", "int", "arithmetic", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. The local result was returned safely to the main program.",
      },
      independent: {
        title: "Independent task",
        instructions: "Create global score = 0. Define add_points(points), use Python's global declaration and add points to score. Read two integer point values, call the procedure for each, then print score.",
        starterCode: "score = 0\n\n# Define add_points, including the global declaration\n",
        exampleInput: ["10", "5"],
        exampleOutput: "15",
        hiddenTests: [
          { input: ["0", "7"], expectedOutput: "7", expectedVariables: { score: 7 } },
          { input: ["12", "8"], expectedOutput: "20", expectedVariables: { score: 20 } },
        ],
        requiredConstructs: ["function", "parameter", "global", "input", "int", "update", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. Your procedure explicitly updated the shared global score for every pair of calls.",
      },
      review: {
        question: "State which variables are automatically local to a sub-program.",
        options: ["Only constants", "Its parameters", "Every variable in the whole file", "Only values printed by it"],
        answer: 1,
        correctFeedback: "Correct. Parameters belong to the sub-program's local scope.",
      },
      retrievalPrompt: "Give one reason to prefer a parameter over a global variable.",
      examCallout: "OCR may ask where a variable can be accessed or require the global keyword. Precise scope explanations score better than simply saying a variable is inside or outside.",
    }),
    buildLesson({
      slug: "functions-arrays-as-parameters",
      title: "Passing and returning arrays",
      shortDescription: "Process a collection inside a sub-program without relying on global data.",
      objective: "Pass an array into a sub-program, iterate over its indices and return a processed array or value.",
      estimatedMinutes: 31,
      specReference: "2.2.3",
      vocabulary: [
        { term: "Array parameter", definition: "A parameter that receives an array or Python list from the caller." },
        { term: "Index", definition: "A zero-based position used to access an array element." },
        { term: "Return array", definition: "Send a complete processed array back from a function." },
        { term: "Mutable", definition: "Able to be changed after creation; Python lists are mutable." },
      ],
      explanation: "An array can be supplied as a parameter just like one number or string. The sub-program can inspect each element, calculate from it or build a new array to return. Passing the collection keeps the sub-program reusable and avoids a global array. Indices begin at 0, so an ERL loop normally runs from 0 to array.length - 1; Python uses range(len(array)).",
      keyPoints: [
        "Arrays can be passed to procedures and functions.",
        "A function may return an array as its result.",
        "Use the loop counter as the array index.",
        "For a length of 5, valid indices are 0 to 4.",
        "Python lists passed to functions can also be modified in place because they are mutable.",
      ],
      examTip: "For an ERL array loop, write for index = 0 to values.length - 1 and access values[index] using the same counter name.",
      commonMistake: "Starting at index 1 misses the first element. Also do not write values[item] in Python when item is already the element value rather than an index.",
      examples: [
        {
          title: "Return doubled values",
          context: "Build and return a new array from every element of the input array.",
          erl: 'function doubleValues(numbers)\n    array result[5]\n    for index = 0 to numbers.length - 1\n        result[index] = numbers[index] * 2\n    next index\n    return result\nendfunction\n\narray original[5]\noriginal[0] = 1\noriginal[1] = 2\noriginal[2] = 3\noriginal[3] = 4\noriginal[4] = 5\ndoubled = doubleValues(original)\nprint(doubled)',
          python: 'def double_values(numbers):\n    result = []\n    for number in numbers:\n        result.append(number * 2)\n    return result\n\noriginal = [1, 2, 3, 4, 5]\ndoubled = double_values(original)\nprint(doubled)',
          notes: [
            { line: "1", explanation: "numbers receives the complete collection." },
            { line: "2–5", explanation: "Each element is doubled and stored in a result collection." },
            { line: "6", explanation: "The complete processed collection is returned." },
          ],
        },
        {
          title: "Return a total",
          context: "Pass an array to a function and add every indexed value.",
          erl: 'function totalScores(scores)\n    total = 0\n    for index = 0 to scores.length - 1\n        total = total + scores[index]\n    next index\n    return total\nendfunction\n\narray scores[3]\nscores[0] = 4\nscores[1] = 6\nscores[2] = 5\nprint(totalScores(scores))',
          python: 'def total_scores(scores):\n    total = 0\n    for index in range(len(scores)):\n        total = total + scores[index]\n    return total\n\nscores = [4, 6, 5]\nprint(total_scores(scores))',
          notes: [
            { line: "2", explanation: "The accumulator is initialised once before the loop." },
            { line: "3–4", explanation: "The counter is used as the index for every element." },
            { line: "6", explanation: "The final total is returned after the loop finishes." },
          ],
        },
      ],
      predict: {
        language: "python",
        code: "def first_plus_last(values):\n    return values[0] + values[len(values) - 1]\n\nnumbers = [3, 7, 4, 9]\nprint(first_plus_last(numbers))",
        expectedOutput: "12",
      },
      debug: {
        language: "python",
        code: "def total_scores(scores):\n    total = 0\n    for index in range(1, len(scores)):\n        total = total + scores[index]\n    return total\n\nprint(total_scores([4, 6, 5]))",
        errorKind: "logic",
        expectedFix: "def total_scores(scores):\n    total = 0\n    for index in range(len(scores)):\n        total = total + scores[index]\n    return total\n\nprint(total_scores([4, 6, 5]))",
        feedback: "range(1, ...) starts at index 1 and misses the first score. range(len(scores)) covers index 0 to length - 1.",
      },
      complete: {
        language: "erl",
        instructions: "Complete the upper bound so every valid index is processed without going out of range.",
        codeParts: ["for index = 0 to scores.length ", "\n    print(scores[index])\nnext index"],
        answers: [["- 1", "-1"]],
      },
      guided: {
        title: "Scaffolded practice",
        instructions: "Define double_values(numbers). Build a new list by doubling each number and return it. Read three integers into numbers, call the function, store its returned list in a main-program variable named doubled and print doubled.",
        starterCode: 'def double_values(numbers):\n    result = []\n    for number in numbers:\n        # Append the doubled value\n        \n    return result\n\nfirst = int(input("First: "))\nsecond = int(input("Second: "))\nthird = int(input("Third: "))\nnumbers = [first, second, third]\ndoubled = double_values(numbers)\nprint(doubled)',
        exampleInput: ["1", "2", "3"],
        exampleOutput: "[2, 4, 6]",
        hiddenTests: [
          { input: ["0", "5", "-2"], expectedOutput: "[0, 10, -4]", expectedVariables: { doubled: "[0, 10, -4]" } },
          { input: ["7", "7", "7"], expectedOutput: "[14, 14, 14]", expectedVariables: { doubled: "[14, 14, 14]" } },
          { input: ["12", "-3", "1"], expectedOutput: "[24, -6, 2]", expectedVariables: { doubled: "[24, -6, 2]" } },
        ],
        requiredConstructs: ["function", "parameter", "return", "return-used", "input", "int", "list", "loop", "append-in-loop", "append", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. Your function processed and returned every array element.",
      },
      independent: {
        title: "Independent task",
        instructions: "Define total_scores(scores). Use a for loop with range(len(scores)) and indexed access to return the total. Read three integer scores into a list, call the function, store the returned value in result and print result.",
        starterCode: "",
        exampleInput: ["4", "6", "5"],
        exampleOutput: "15",
        hiddenTests: [
          { input: ["0", "0", "0"], expectedOutput: "0", expectedVariables: { result: 0 } },
          { input: ["10", "2", "8"], expectedOutput: "20", expectedVariables: { result: 20 } },
          { input: ["-2", "5", "1"], expectedOutput: "4", expectedVariables: { result: 4 } },
        ],
        requiredConstructs: ["function", "parameter", "return", "return-used", "input", "int", "list", "for", "len", "index-in-loop", "index", "update-in-loop", "update", "print"],
        restrictedPatterns: ["no-hardcoded-answer"],
        successMessage: "Correct. You passed the list, visited every zero-based index and returned the total.",
      },
      review: {
        question: "Identify the final valid index of an array whose length is 6.",
        options: ["4", "5", "6", "7"],
        answer: 1,
        correctFeedback: "Correct. Zero-based indices for six elements are 0, 1, 2, 3, 4 and 5.",
      },
      retrievalPrompt: "State one benefit of passing an array as a parameter rather than making it global.",
      examCallout: "OCR commonly combines arrays, loops and sub-programs. Use the counter as the index, stop at length - 1 and return the requested value rather than only printing it.",
    }),
  ],
};
